<h1>Review</h1>
<%= simple_form_for([@restaurant, @review]) do |f| %>
    <%= f.input :content %>
    <%= f.input :rating %>
    <%= f.button :submit %>
<% end %>

<%= link_to "Back to #{@restaurant.name}", restaurant_path(@restaurant) %>
